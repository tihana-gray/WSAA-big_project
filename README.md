# WSAA-big_project

# Sales Dashboard Project

## Setup

1. Clone the repository
2. Install required libraries:
   pip install pandas
3. Run:
   python create_db.py

This will create the SQLite database (deals.db)